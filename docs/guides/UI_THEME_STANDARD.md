# UI 设计系统标准 (弹窗与按钮)

> **核心理念**：
> 将 **逻辑** (Vue 组件) 与 **样式** (主题注册表) 分离。
> UI 变更应发生在注册表中，而不是组件逻辑中。

---

## 🏗 系统架构

我们拥有两个协同工作的并行系统：

1.  **弹窗系统 (Modal System)**：控制全局容器样式（背景、模糊、吉祥物、动画）。由 **套装 (Suites)** 管理。
2.  **按钮系统 (Button System)**：控制原子交互元素。由 **主题 (Themes)** 管理。

---

## 1. 按钮系统 (原子层)

按钮是设计中的原子。它们是“无逻辑”组件，根据 `themeId` 改变外观。

-   **组件**: `<BaseButton>` (无逻辑，纯 UI)
-   **注册表**: `utils/buttonThemeRegistry.ts`

### 按钮主题目录

| 主题 ID (Theme ID) | 描述 | 使用位置 | 用例 |
| :--- | :--- | :--- | :--- |
| `primary` | 蓝紫色渐变 | 弹窗、登录表单 | 主要操作 (确认、登录、提交) |
| `suit-001-primary` | 霓虹蓝玻璃 (Suite 001) | 个人中心弹窗 | 个人中心弹窗的 "保存/确认" 按钮 |
| `suit-001-secondary` | 透明边框 (Suite 001) | 个人中心弹窗 | 个人中心弹窗的 "取消" 按钮 |
| `secondary` | 幽灵/轮廓风格 | 弹窗 | 次要操作 (取消、返回) |
| `destructive` | 红色/警告 | 删除弹窗 | 高风险操作 (退出登录、注销账号) |
| `marketing-buy` | 蓝色 -> 悬停橙色 | 首页 / 商品卡片 | 商品卡片上的“立即购买”或“查看详情” |
| `social-google` | 玻璃/白色 | 登录弹窗 | Google OAuth 登录按钮 |
| `tab` | 透明 | 登录弹窗 | 在“登录”和“注册”之间切换 |

### 如何使用

```vue
<!-- 标准主要操作 -->
<BaseButton themeId="primary" @click="save">保存更改</BaseButton>

<!-- 营销按钮 (首页) -->
<BaseButton themeId="marketing-buy" block>立即购买</BaseButton>
```

---

## 2. 弹窗系统 (容器层)

弹窗是容器。它们被组织成 **套装 (Suites)**。一个套装定义了弹窗的整体“氛围”。

-   **组件**: `BaseModal.vue` (及其扩展类型如 `BaseConfirmModal`)
-   **注册表**: `utils/modalThemeRegistry.ts`

### 套装目录

| 套装 ID (Suite ID) | 名称 | 视觉风格 | 吉祥物位置 |
| :--- | :--- | :--- | :--- |
| `suit-001` | **经典幽灵 (Classic Phantom)** | 深蓝玻璃 + 模糊 | 底部居中 |
| `suit-002` | **柔光风格 (Soft Light)** | 明亮/暖色玻璃 | 左侧 |
| `suit-003` | **赛博朋克 (Cyberpunk)** | 高对比度霓虹 | 右侧 |

### 如何使用

```vue
<!-- 使用默认套装 (suit-001) -->
<BaseModal :visible="true" title="你好" />

<!-- 强制使用特定套装 -->
<BaseModal :visible="true" themeId="suit-002" title="你好" />
```

---

## 🔗 映射关系：套装 vs. 主题

该系统的强大之处在于它们的连接方式。目前，我们采用 **松耦合** 策略。

### 当前映射策略 (全局)

*   **所有弹窗** (无论属于哪个套装) 默认使用以下按钮映射：
    *   **确认操作** -> `primary` 按钮主题
    *   **取消操作** -> `secondary` 按钮主题

> **未来优化 (严格套装)**：
> 如果我们要让“套装 2”的弹窗拥有特定的“套装 2 按钮”（例如：橙色按钮而不是蓝色），我们将更新 `BaseModal.vue` 以根据弹窗套装动态解析按钮主题。
>
> *示例 (概念)*：
> ```typescript
> // 逻辑概念
> const confirmButtonTheme = computed(() => {
>    if (props.themeId === 'suit-002') return 'primary-warm'; // 假设的橙色主题
>    return 'primary';
> })
> ```

---

## 📍 业务位置映射

| 页面 / 区域 | 组件 | 样式来源 | 使用的主题/套装 |
| :--- | :--- | :--- | :--- |
| **首页 (Home Page)** | 商品卡片 | `BaseButton` | `marketing-buy` |
| **全局 (Global)** | **登录弹窗** | `BaseButton` | `primary` (提交), `social-google` (OAuth), `tab` (切换) |
| **全局 (Global)** | **退出弹窗** | `BaseConfirmModal` | `suit-001` (容器) + `primary`/`secondary` (按钮) |
| **个人中心** | **编辑资料** | `BaseFormModal` | `suit-001` (容器) + `primary` (按钮) |

---

## 🛠 如何修改样式

### 场景 A: 全局修改“Primary”按钮颜色
1.  打开 `components/shared/BaseButton.vue`。
2.  找到 `.btn-primary` 类。
3.  修改 `background: linear-gradient(...)`。
4.  **结果**: 所有弹窗、登录表单和确认对话框将立即更新。

### 场景 B: 创建一个新的“Suite 4”弹窗
1.  打开 `utils/modalThemeRegistry.ts`。
2.  添加 `suit-004` 配置 (吉祥物图片、动画)。
3.  打开 `components/shared/BaseModal.vue`。
4.  添加 `.variant-suite-004` CSS 类以进行特定的玻璃效果调整。

### 场景 C: 创建一个新的按钮类型
1.  打开 `utils/buttonThemeRegistry.ts`。
2.  添加定义: `'vip-gold': { id: 'vip-gold', variantClass: 'btn-vip-gold' }`。
3.  打开 `components/shared/BaseButton.vue`。
4.  添加 CSS: `.btn-vip-gold { background: gold; color: black; }`。
